# Codex 完成计划的文档标签

2026-09-27。本阶段将 Codex Core 的 `item_completed` / `item.type = plan` 保存为任务运行记录中的完整计划文档。它与 `update_plan` 的步骤进度是两类事件：后者仍在会话时间线中显示，不再误用作任务摘要里的计划文档。

任务摘要显示最近一次完成计划的标题；点击后在当前主窗口或独立任务窗口打开文档标签。文档支持 Markdown 阅读和复制，并参与标签关闭、重新打开、固定、移动及重启恢复。恢复时校验任务归属和对应运行记录，避免从其他任务打开同名计划标签。项目或工作树切换时，独立窗口保留有效的任务计划文档标签。

验证：`script/build_and_run.sh --build-app` 成功；计划事件解析、保存、两种窗口的标签归属与恢复，以及相邻标签回归共 38 项测试通过。新增测试后的计划和独立任务窗口专项 12 项再次通过；`git diff --check` 通过。桌面锁屏阻止了本阶段的原生点击验收，实际模型返回完整计划、Markdown 链接行为与当前 Codex Mac 的视觉和键盘行为仍待逐项配对。

后续链接路由：计划文档现与会话回复共用任务工作区文件解析、内置浏览器偏好和链接上下文操作。签名应用重新构建成功，计划、Markdown 链接和独立任务标签的 23 项相关测试通过。系统剪贴板专项在锁屏环境下读取为空，未计入通过；计划链接的原生点击仍待验收。
