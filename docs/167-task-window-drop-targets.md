# 独立任务窗口投放区域

2026-09-19。本轮按用户要求整理本地代码并提交，真实系统拖放验收尚未完成。

独立任务窗口增加隐藏侧面板与底部面板的投放区域，以及有效落点高亮。侧面板目标跟随左右布局交换；底部仅接受终端。投放到文件面板时切换为内容面板，移动继续复用原网页或终端实例。拖动状态按窗口隔离，在完成、取消、关闭源标签、重置项目和视图退出时清理；释放检测仅在拖动期间运行，旧会话不能清除新会话状态。

新增 6 项测试覆盖隐藏面板展开、网页和真实 PTY 实例保留、无效及跨窗口投放拒绝、过期清理、源关闭、项目重置和文件面板替换。相关窗口、标签、固定项及终端焦点回归共 50 项通过，0 失败，日志为 `.cache/task-window-drop-regression.log`。

通过 `script/build_and_run.sh` 构建启动并通过严格签名检查。原生辅助功能检查确认原工作区正常显示，点击任务输入框后取得焦点，原待发送草稿保持不变，没有停留在恢复工作区加载状态。

以上原生检查只证明启动和基础交互正常；实际鼠标拖放、长时间拖动、窗口外释放、Escape 取消以及交换布局后的目标命中仍待专门验收。主窗口隐藏投放区域、跨窗口移动及完整 Codex 配对不在本轮完成范围。缓存、日志和个人运行数据不提交。
