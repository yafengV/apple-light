# Codex 上下文整理时间线标记

2026-09-26。Codex Core 的 `context_compacted` 事件现在进入当前回合的有序时间线，在主窗口和独立任务窗口共用的回复视图显示“上下文已整理”标记。手动 `/compact` 的结果不再伪装为普通助手回复；自动整理发生在普通回合中时，前后回复仍分开显示，多次整理也各有一条标记。标记和原有消息顺序会持久化；旧记录继续按原格式读取。

本地回归覆盖两次自动整理之间的文本、持久化编解码、手动 `/compact` 的真实 Core 事件，以及既有回复搜索与旧记录读取，共 11 项定向测试通过。`script/build_and_run.sh --build-app` 构建通过。Mac 锁屏使时间线卡片的实际字体、焦点、滚动和当前 Codex Mac 的视觉/交互配对仍未验收；43 类完整配对数量不变。
