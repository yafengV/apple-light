# Codex 回合差异时间线

2026-09-26。固定版 Codex Core 发出的累计 `turn_diff` 现进入任务时间线，按回合维护一张可展开的“本轮代码变更”卡。后续差异事件原位更新同一卡片，空差异事件会移除它；卡片显示文件数、可选择和复制的统一差异文本。为避免工作区记录被异常大的事件撑满，内联文本最多保留 262,144 个字符，截断时明确提示且文件总数仍按完整事件计算。

本地假模型经真实 Core 执行 `apply_patch` 后，集成测试确认补丁卡、差异卡和最终回复的顺序，包含实际新增文件及内容；应用重启后差异与时间线保持一致。边界测试覆盖重复累计更新、同卡 ID、文件数、序列化与空差异清除。

目前卡片只展示 Core 提供的回合快照，不替代项目 Git 审查页，也不代表当前 Codex Mac 的差异视觉、键盘与文件导航已配对。Mac 锁屏期间无法完成原生验收；43 类完整配对数量不变。
