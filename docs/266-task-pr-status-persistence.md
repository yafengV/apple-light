# PR 状态刷新与任务记录

2026-09-27。任务内 PR 详情从 GitHub 读取最新信息后，ShipiOS 现在将标题、草稿标记、开放/关闭/合并状态及检查时间写回原任务。返回摘要时，列表使用这份最近确认的状态；旧版记录没有状态字段时仍可读取，并明确显示为“上次记录”。创建或从开放 PR 列表发现的记录标记为开放。

写回只更新该任务已经关联、编号和 URL 一致的 PR；其他任务、失效任务或不同 PR 不被创建或覆盖。网络与认证失败不会改写旧状态。此状态是最后一次成功检查的快照，不替代 GitHub 的实时查询；用户仍可进入详情刷新。

验证：`script/build_and_run.sh --build-app` 构建和签名校验成功；PR 回归 20 项、摘要相关 7 项测试通过，覆盖旧 JSON 迁移、任务隔离、状态写回与重启恢复。Mac 锁屏继续阻止摘要返回、状态视觉和焦点的原生配对。
