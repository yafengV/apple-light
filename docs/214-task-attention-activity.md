# 待处理任务提示一致性

2026-09-26。任务的待处理状态现由同一处计算，覆盖工具审批、Codex 结构化提问、MCP 表单或 URL 请求，以及未读活动。侧栏为 MCP 请求显示独立的待完成图标；“下一个需关注任务”与宠物活动菜单使用同一任务集合，清除未读后仍保留需要用户操作的任务。宠物在运行回合等待用户操作时显示“等待操作”，不再只显示“正在运行”。

15 项导航和宠物相关测试通过，覆盖各待处理类型、顺序、未读清除与状态恢复。侧栏图标、宠物菜单和键盘导航的当前 Codex Mac 原生可见配对仍待解锁后验收。
