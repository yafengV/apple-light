# 任务内 Pull request 详情

2026-09-27。Codex Mac 任务摘要的 PR 行会进入当前窗口的 PR 详情侧栏。ShipiOS 的任务摘要现在点击已记录 PR 后，在同一摘要位置显示详情与“返回摘要”，而不是直接跳出到浏览器。详情先显示已保存的标题和分支，再用 GitHub CLI 的 `gh pr view --repo` 读取当前状态、正文、审查决定、CI 检查和合并冲突；可刷新、复制链接或在所属窗口浏览器中打开。

刷新结果必须与任务保存的 PR 编号、GitHub 仓库及 URL 一致。网络、认证或本地 `gh` 缺失时保留已保存内容、显示错误并允许重试；不把失败误报为 PR 已关闭。主窗口和独立任务窗口共用摘要视图，详情不会切换后台主任务。

验证：`script/build_and_run.sh --build-app` 构建及签名校验成功；本地 GitHub CLI 模拟服务验证合并状态、正文、审查/检查汇总、仓库身份拒绝。完整 PR 回归 19 项、摘要相关 7 项测试通过。Mac 锁屏仍阻止两端页面、返回、焦点和键盘的原生配对。
