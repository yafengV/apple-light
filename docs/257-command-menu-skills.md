# 命令菜单技能入口

2026-09-26。本机安装的 Codex 客户端命令目录包含 `openSkills` 和 `forceReloadSkills`，归于技能组。ShipiOS 现在提供“打开技能”和“重新加载技能”：前者在主窗口内打开设置的技能管理子页，后者重新读取 ShipiOS 独立目录中的插件与技能，并更新输入区候选。独立任务窗口的命令菜单也可调用；重新加载不会把后台主窗口切到前台。

技能管理子页在未安装技能时仍可进入并显示空状态，删除最后一个技能后保留该页，同时清除旧搜索高亮。全局关闭插件时，两个命令不可用。真实 Codex 的技能目录、安装来源和原生焦点行为仍待逐项对照。

41 项相关 Swift 回归通过，覆盖空状态导航、原有页面返回、外部安装后命令重新读取技能、命令分组、全局关闭状态、独立技能和设置布局。另有 10 项原生粘贴板与本地模型服务测试在正常 macOS 权限下通过。额外启动的全量 Swift 回归因耗时在工作树组结束前中断；中途发现的两处旧导航断言已更新，并包含在上述 41 项通过结果中，因此不将全量回归记为通过。

`script/build_and_run.sh --build-app` 构建成功，`codesign --verify --deep --strict` 通过。Mac 当前锁屏，原生界面验收仍未完成。
