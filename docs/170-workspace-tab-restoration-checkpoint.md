# 主窗口普通标签恢复：阶段性提交

2026-09-19。按用户要求先整理提交当前本地代码。本阶段尚未完成原生验收，不能据此认定标签恢复或 Codex UI 对齐完成。

## 当前实现

主窗口按任务保存网页、终端和审查标签的稳定 ID、顺序、面板位置、选中项、面板显隐、左右方向及审查范围。网页保存已提交 URL 与地址输入草稿；恢复终端时创建新 shell，不重放旧命令。切换任务时按需恢复，保留未访问任务的缓存，并防止退出清理资源后覆盖已有布局。

旧工作区缺少布局字段时正常加载；损坏的可选布局缓存被忽略，不阻止任务与草稿加载。重复标签、无效 ID 和不允许的面板位置有防御处理。删除归档任务时同步移除其布局。固定标签引用复用恢复后的实例。分离标签的窗口路由已接线，但完整原生恢复仍待验证。

## 已有验证

- 新增 10 项恢复测试，覆盖磁盘往返、稳定 ID、新 shell、任务隔离、关闭后不复活、固定引用复用、异常缓存、分离窗口路由、真实异步启动退出加载状态，以及切换项目之前保存面板。
- 153 项相关回归通过，0 失败，日志 `.cache/tab-layout-regression.log`；该日志不提交。
- 隔离原生实例正常退出后，磁盘保存了网页、终端、审查和地址草稿共四个标签及各自位置。重新启动后，标签、地址草稿和面板选中项恢复；终端显示新的 shell 提示符。原生 PID/环境变量对照尚未完成，不能用提示符代替该项验收。
- 最终构建及严格签名检查通过，日志 `.cache/tab-layout-checkpoint-build.log`。已恢复原数据目录，原任务与待发送草稿保留，点击输入框后取得焦点；工作区没有停留在恢复加载页。

## 明确待修与待验收

原生重启后，右侧网页标签的标题和 URL 已恢复，本地 HTTP 服务也收到请求，但网页内容区域显示空白，辅助功能树没有页面正文。原因尚未确定，需继续检查 WebView 恢复与视图挂载生命周期；本次提交保留该已知问题。

独立任务窗口的普通标签布局尚未接入本次持久化。分离窗口、非当前任务窗口恢复、网页表单/滚动/浏览历史，以及全部 Codex 页面与交互配对仍未完成。自动化通过不能替代这些原生验收。

本次只提交源码、测试和文档，缓存、构建产物、运行数据、凭据及个人配置均不提交。
